# Production env - loaded by react-scripts during `npm run build`
# (NODE_ENV=production, which is what Vercel uses). Public values only, safe
# to commit. Vercel project env vars override these.
REACT_APP_API_URL=https://chatmoo-official.onrender.com
REACT_APP_ADMIN_API_URL=https://chatmoo-official.onrender.com