# Production env - loaded by react-scripts during `npm run build`
# (NODE_ENV=production, which is what Vercel uses) and when running the
# deployment preview builds.
#
# These are all PUBLIC values (URLs + PayMongo TEST public key), so it's safe
# to commit. Vercel project env vars override these; set the same keys there
# if you prefer keeping values out of git.

REACT_APP_API_URL=https://chatmoo-official.onrender.com
REACT_APP_SOCKET_URL=https://chatmoo-official.onrender.com
REACT_APP_PAYMONGO_PUBLIC_KEY=pk_test_ey6fZVweocUnY8aJ7wreQH9U